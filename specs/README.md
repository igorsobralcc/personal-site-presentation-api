# Feature specifications

Create one directory per independently deliverable API behavior:

```text
specs/
  profile-management/
    spec.md
  public-presentation/
    spec.md
```

Use `_template/spec.md`. Specifications are permanent product documentation,
not temporary planning notes, and evolve through `Draft`, `Approved`, and
`Implemented` states.
